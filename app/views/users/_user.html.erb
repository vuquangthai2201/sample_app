<li>
  <%= gravatar_for user, size: Settings.users.gravatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".delete_text"), user, method: :delete,
        data: { confirm: t(".confirm_text") } %>
  <% end %>
</li>
